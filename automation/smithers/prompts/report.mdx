REPORT PHASE

Goal:
{props.goal}

UI allowed:
{String(props.allowUi)}

Plan:
{props.plan}

Implementation:
{props.implementation}

Validation:
{props.validation}

Review:
{props.review}

Review fix:
{props.reviewFix}

Tasks:
- Summarize what was delivered and what remains.
- Include skills that were actually applied and any expected-skill gaps.
- Include checkpoint and validation summaries explicitly.
- Use `completed` only when acceptance criteria are met, blocking validation commands passed, and chunk checkpoints were recorded.
- Use `needs-human` when unresolved blockers or risks remain.
- Recommend next actions that can be executed by agents.

Return JSON matching the output schema exactly.
